{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: core defaults plus native open-file dialog only.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
